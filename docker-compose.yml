version: '3'
services:
  api:
    build: /Tasks/hello_world
    ports:
      - "8080:80"


  redis-server:
    image: redis:alpine
    networks:
    - backend

  db:
    image: postgres:alpine
    networks:
    - backend

  # vote:
  #   image: voting-app
  #   ports:
  #     - 5000:80
  #   depends_on:
  #     - redis
  #   networks:
  #     - backend
  #     - frontend

  # result:
  #   image: result-app
  #   ports:
  #     - 5001:80
  #   links:
  #     - db
  #   networks:
  #     - backend
  #     - frontend

  # worker:
  #   image: worker
  #   links:
  #     - redis
  #     - db
  #   networks:
  #     - backend

networks:
  backend:
  frontend: